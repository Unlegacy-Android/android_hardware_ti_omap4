# Hardware tunables
type sysfs_hardware, fs_type, sysfs_type;

# Support asec containers getting mounted (Android 4.4)
ifndef(`android_api_lp_or_later', `
  allow file_type rootfs:filesystem associate;
')
